apiVersion: apps/v1
kind: Deployment
metadata:
  name: poc-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: poc-app
  template:
    metadata:
      labels:
        app: poc-app
    spec:
      containers:
      - name: poc-app
        image: kathiradmin/poc-app:latest
        ports:
        - containerPort: 3000
